DEBUG emulation — APPLY FIX phase.

Confirmed: {{confirmedHypothesisId}}
Proposal: {{fixProposal}}

Apply the minimal fix. Keep instrumentation sentinels (// DEBUG-INSTRUMENT:{{runId}}) in place.

Reply when done with:
```json
{"applied":true,"files":["path"]}
```
